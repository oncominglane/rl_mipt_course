Лекция 11
Обучение с подкреплением

Никита Юдин, iudin.ne@phystech.edu
Московский физико-технический институт
Физтех-школа прикладной математики и информатики

16 апреля 2026

Никита Юдин (МФТИ)

Лекция

16 апреля 2026

1 / 24

Deep Deterministic Policy Gradient (DDPG)
Схема DQN имела принципиальный недостаток: мы не могли работать
с непрерывными пространствами действий в силу необходимости
постоянно считать операторы максимума и аргмаксимума
max Qθ (s, a)
a

как для жадного выбора действия, так и для построения таргета в
задаче регрессии.
Единственная возможная архитектура модели — приём действий на
вход вместе с состояниями, тогда поиск аргмаксимума проводить, в
целом, можно, но дорого: инициализируем a0 случайно, и устраиваем
градиентный подъём по входу в модель:
ak+1 = ak + α ∇a Qθ (s, a)|a=ak .
Никита Юдин (МФТИ)

Лекция

16 апреля 2026

2 / 24

Итак, пусть πω (s) принимает на вход состояние s и выдаёт
аргмаксимум текущей аппроксимации Q-функции, то есть будем
добиваться πω (s) ≈ argmax Qθ (s, a). Понятно, как обучать такую сеть:
a

Qθ (s, πω (s)) → max .
ω

Весь алгоритм DQN оставляем неизменным с единственной
модификацией, что на каждом батче также нужно сделать шаг
оптимизации ω. При этом каждый раз, когда в схеме необходимо
считать максимум или аргмаксимум Qθ , используется πω (s).
В стандартном алгоритме DQN нам было необходимо считать
max
Qθ − (s ′ , a′ ), и в дефолтной версии алгоритма использовалась
′
a

таргет-сеть. Технически это означает, что для таргет-сети Qθ − (s ′ , a′ )
нам тоже нужно знать аргмаксимум, поэтому можно хранить старую
версию вспомогательной функции πω − (s).
Никита Юдин (МФТИ)

Лекция

16 апреля 2026

3 / 24

Итого мы получили, что для жадного выбора действия используется
πω (s) (отсюда такое обозначение этой «вспомогательной» функции —
это фактически стратегия); а таргет для перехода T := (s, a, r , s ′ )
вычисляется по формуле
y (T) := r + γQθ − (s ′ , argmax Qθ − (s ′ , a′ )) ≈ r + γQθ − (s ′ , πω − (s)).
a′

Такой алгоритм называется Deep Deterministic Policy Gradient (DDPG),
и название может сбить с толку: а причём здесь policy gradient?

Никита Юдин (МФТИ)

Лекция

16 апреля 2026

4 / 24

Теорема 1 — Deterministic Policy Gradient: В непрерывных

пространствах действий в предположении дифференцируемости
Q-функций по действиям:

∇θ J(πθ ) =

1
E
(∇θ πθ (s))⊤ ∇a Q π (s, a)|a=πθ (s) .
1 − γ dπθ (s)

(1)

Доказательство.

∇θ V πθ (s) = {VQ уравнение} = ∇θ Ea∼πθ (s) Q πθ (s, a) = ∇θ Q πθ (s, πθ (s)) = (∗).

Заметим, что в последнем выражении при малом изменении θ
поменяется не только πθ (s), но и сама оценочная функция Q πθ .
Считая, что якобиан функции Rn → Rm имеет размерность n × m, и
обозначая размерность действий как A, а размерность параметров θ
буквой d, получаем следующие размерности матриц и векторов:

∇θ Q πθ (s, a) ∈ Rd ×1 ,
Никита Юдин (МФТИ)

∇a Q πθ (s, a) ∈ RA×1 ,
Лекция

∇θ πθ (s) ∈ RA×d .
16 апреля 2026

5 / 24

Тогда продолжение вычисления выглядит так:
(∗) = (∇θ πθ (s))⊤ ∇a Q π (s, a)|a=πθ (s) + ∇θ Q πθ (s, a)|a=πθ (s) ,
где последнее слагаемое — градиент Q πθ при фиксированном a по
параметрам стратегии πθ , которую он оценивает. Отдельно это
слагаемое имеет вид:

∇θ Q πθ (s, a) = {QV уравнение } = γEs ′ ∇θ V πθ (s ′ ).
Получаем рекурсивную формулу, аккуратно собирая которую, получим:

∇θ V πθ (s) = ET ∼π |s0 =s ∑ γt (∇θ πθ (st ))⊤ ∇a Q π (st , a)|a=πθ (st ) .
t ≥0

Осталось только применить теорему об эквивалентной форме
мат.ожидания по траекториям для
f (s, a) = (∇θ πθ (s))⊤ ∇a Q π (s, a)|a=πθ (s) .
Никита Юдин (МФТИ)

Лекция

16 апреля 2026

■
6 / 24

Сразу построим суррогатную функцию для такой формулы градиента:

Lπ̃ (θ) :=

1
Q π̃ (s, πθ (s)).
E
1 − γ dπ̃ (s)

Действительно, если мы посчитаем градиент этой функции по θ, то
мы просто получим формулу chain rule для оптимизации параметров
стратегии через градиент Q-функции по действиям. Иными словами,
градиент по параметрам детерминированной стратегии указывает
просто проводить policy improvement: выбирать те действия, для
которых Q-функция больше, используя её градиент по действиям.
Если мы хотим построить Actor-Critic схему, воспользовавшись такой
формулой, нам придётся аппроксимировать Q-функцию
Qω (s, a) ≈ Q π (s, a) и явно использовать её градиент по действиям,
надеясь на то, что ∇a Qω (s, a) ≈ ∇a Q π (s, a).

Никита Юдин (МФТИ)

Лекция

16 апреля 2026

7 / 24

Итого, будем сэмплировать батч состояний из реплей буфера и делать
шаг градиентного подъёма:
θ ← θ + αEs (∇θ πθ (s))⊤ ∇a Q π (s, a)|a=πθ (s) ,
где состояния s приходят из произвольного распределения (например,
из реплей буфера). Это эквивалентно одному шагу градиентной
оптимизации суррогатной функции:
Es Q π (s, πθ (s)) → max .

(2)

θ

Q-функцию Qω (s, a) ≈ Q π (s, a), необходимую для такой оптимизации,
будем тоже учить в off-policy режиме с одношаговых оценок: ему для
данной пары s, a требуется лишь сэмпл s ′ , поэтому такого критика
можно обучать по переходам T := (s, a, r , s ′ ) из буфера на таргеты

y (T) := r + γQω − s ′ , π(s ′ ) .
Никита Юдин (МФТИ)

Лекция

16 апреля 2026

8 / 24

Теорема
Предыдущие две схемы (вывод через DQN и через Policy Gradient)
эквивалентны полностью.
Доказательство. Методом пристального взгляда.

■

В рассмотренной схеме из-за использования детерминированной
стратегии, как и в DQN, возникает проблема exploration-exploitation-а.
В непрерывных пространствах действий вместо ε-жадной стратегии
возможно добавлять к выбранным стратегией действиям шум из
нормального распределения:
at := π(st ) + ε t ,

Никита Юдин (МФТИ)

Лекция

ε t ∼ N (0, σ2 I ).

16 апреля 2026

9 / 24

Пример
Если действия робота — это направление движения (например,
поворот руля управляемой машины), а один шаг в среде это доля
секунды, странно проводить исследования, рандомно «подёргиваясь»
пару раз в секунду. Хочется целенаправленно смещать траекторию:
если мы решили в целях исследования повернуть руль чуть правее,
чем говорит наша детерминированная стратегия, следует сохранить
это смещение руля вправо и в дальнейшем. Для моделирования этого
шум должен быть скоррелированным: поэтому вместо независимого
шума имеет смысл добавлять случайный процесс, колеблющийся
вокруг нуля.
Определение
Шум Орнштейна — Уленбека (Ornstein–Uhlenbeck noise), в начале
эпизода инициализированный нулём, задаётся рекурсивно как:
ε t+1 := αε t + N (0, σ2 I ), где α ≤ 1 и σ — гиперпараметры.
Никита Юдин (МФТИ)

Лекция

16 апреля 2026

10 / 24

Алгоритм 1: Deep Deterministic Policy Gradient (DDPG)
Гиперпараметры: B — размер мини-батчей, β — коэф.
экспоненциального сглаживания для таргет-сеток, α, σ —
параметры шума, Qθ (s, a) — нейросетка с параметрами θ,
πω (s) — детерминированная стратегия с параметрами ω, SGDоптимизаторы, K — периодичность обновления весов таргета.
Инициализировать θ, ω произвольно
Положить θ − := θ
Положить ω − := ω
Инициализировать шум ε := 0
Пронаблюдать s0
На очередном шаге t:
1 обновить шум ε ← αε + ε̂, где ε̂ ∼ N (0, σ2 I )
2 выбрать at := πω (st ) + ε
3 пронаблюдать rt , st+1 , donet+1
4 добавить пятёрку (st , at , rt , st+1 , donet+1 ) в реплей буфер
5 засэмплировать мини-батч размера B из буфера
Никита Юдин (МФТИ)

Лекция

16 апреля 2026

11 / 24

6 сделать один шаг градиентного подъёма по ω:
1
Qθ (s, πω (s)) → max
ω
B s∑
∈B
7 для каждого перехода T := (s, a, r , s ′ , done) посчитать таргет:

y (T) := r + γ(1 − done)Qθ − s ′ , πω − s ′
8 сделать один шаг градиентного спуска по θ:
1
(Qθ (s, a) − y (T))2 → min
∑
B T
θ
9 обновить таргет-сети, если t mod K = 0:
θ − ← (1 − β)θ − + βθ
ω − ← (1 − β)ω − + βω
Никита Юдин (МФТИ)

Лекция

16 апреля 2026

12 / 24

Алгоритм 2: Twin Delayed DDPG (TD3)
Гиперпараметры:
B — размер мини-батчей, N —
периодичность обновления весов стратегии, α, σ — параметры
шума, σ̂, c — параметры шума для добавки к действиям
для таргета, β — коэф. экспоненциального сглаживания для
таргет-сеток, Qθ1 (s, a), Qθ2 (s, a) — нейросетки с параметрами
θ1 , θ2 , πω (s) — детерминированная стратегия с параметрами ω,
SGD-оптимизаторы.
Инициализировать θ1 , θ2 , ω произвольно
Инициализировать таргет-сетки θ1− := θ1 , θ2− := θ2 , ω − := ω
Инициализировать шум ε 0 := 0
Пронаблюдать s0
На очередном шаге t:
1 посчитать шум ε t := αε t −1 + ε, где ε ∼ N (0, σ2 I )
2 выбрать at := πω (st ) + ε t
3 пронаблюдать rt , st+1 , donet+1
4 добавить пятёрку (st , at , rt , st+1 , donet+1 ) в реплей буфер
5 засэмплировать мини-батч размера B из буфера
Никита Юдин (МФТИ)

Лекция

16 апреля 2026

13 / 24

6 для каждого перехода T := (s, a, r , s ′ , done) посчитать таргет:
ε′ ∼ clip(N (0, σ̂I ), −c, c)
y (T) := r + γ(1 − done) min Qθ − (s ′ , πω − (s ′ ) + ε′ )
i ∈{1,2}

i

7 сделать один шаг градиентного спуска по θ1 и θ2 :
1
(Qθ1 (s, a) − y (T))2 → min
B∑
θ1
T
1
(Qθ2 (s, a) − y (T))2 → min
B∑
θ2
T

Никита Юдин (МФТИ)

Лекция

16 апреля 2026

14 / 24

8 если t mod N = 0:
• сделать один шаг градиентного подъёма по ω:
1
Qθ1 (s, πω (s)) → max
ω
B s∑
∈B

• обновить таргет-сети:
θ1− ← (1 − β)θ1− + βθ1
θ2− ← (1 − β)θ2− + βθ2
ω − ← (1 − β)ω − + βω

Никита Юдин (МФТИ)

Лекция

16 апреля 2026

15 / 24

Обучение стохастических политик

Определение
Скажем, что для параметризации πθ (a | s) применим
репараметризационный трюк (reparameterization trick), если
сэмплирование a ∼ πθ (a | s) эквивалентно сэмплированию шума из
некоторого не зависящего от параметров распределения ε ∼ p(ε) и его
дальнейшего детерминированного преобразования a = fθ (s, ε).

Никита Юдин (МФТИ)

Лекция

16 апреля 2026

16 / 24

Обучение стохастических политик
Пример 1: Пусть наша стратегия параметризована нормальным

распределением:
πθ (a | s) := N (µθ (s), σθ (s)2 I ).
Тогда для неё применим репараметризационный трюк: сэмплирование
действий эквивалентно a := µθ (s) + ε ⊙ σθ (s), где ε ∼ N (0, I ), ⊙ —
поэлементное перемножение.
Пример
Семейство детерминированных стратегий πθ (s) тоже можно считать
таким «вырожденным» примером параметризаций, для которой
можно проворачивать репараметризационный трюк: просто шум ε
считаем «пустым».
Никита Юдин (МФТИ)

Лекция

16 апреля 2026

17 / 24

Теорема
Если для πθ (a | s) применим репараметризационный трюк, то:

∇θ J(πθ ) =

1
E
E
(∇θ fθ (s, ε))⊤ ∇a Q π (s, a)|a=fθ (s,ε) . (3)
1 − γ dπθ (s) ε∼p(ε)

Доказательство. Полностью полностью повторяет вывод теоремы
1.

Никита Юдин (МФТИ)

Лекция

16 апреля 2026

■

18 / 24

Таким образом, все идеи DDPG расширяются на этот случай. Убирая
из формулы градиента частоты посещения состояний и переходя к
policy iteration схеме, получаем следующий функционал:
Es Ea∼πθ (a|s) Qω (s, a) → max,

(4)

θ

где состояния берутся из буфера. В силу репараметризационного
трюка мы легко справимся со взятием градиента на практике:

∇θ Ea∼πθ (a|s) Qω (s, a) = Eε∼p(ε) ∇θ Qω (s, fθ (s, ε)).

Никита Юдин (МФТИ)

Лекция

16 апреля 2026

19 / 24

Представьте, что вы хотите
объехать дерево. Вы можете объехать его
справа, можете слева. Критик сообщает
вам высокие значения и слева, и справа, и
оптимизируя (4) в классе гауссиан, можно
получить стратегию, которая с наибольшей
вероятностью выбирает действие «врезаться
в дерево».

Пример 2:

Никита Юдин (МФТИ)

Лекция

16 апреля 2026

20 / 24

Пример 3: Например, можно использовать

смесь гауссиан. Тогда при использовании
K компонент смеси актёр для данного состояния
s выдаёт следующие величины: K суммирующихся
в единицу чисел wi (s, θ), а также K векторов
µi (s, θ), σi (s, θ), где i ∈ 1, 2, . . . , K . Итоговое
распределение полагается
K

πθ (a | s) := ∑ wi (s, θ)N (µi (s, θ), σi (s, θ)2 I ).
i=1

Никита Юдин (МФТИ)

Лекция

16 апреля 2026

21 / 24

Утверждение
Градиент (4) по параметрам актёра θ равен

∇θ = Ea∼πθ (a|s) ∇θ ln πθ (a | s) (Qω (s, a) − b(s)) ,
где b(s) — бэйзлайн, произвольная функция от состояний.

Никита Юдин (МФТИ)

Лекция

16 апреля 2026

22 / 24

Теорема (Формула замены переменной в плотности)
Пусть a := g (u), где g : RA → RA , и u ∼ µθ (u | s). Тогда:
ln πθ (a | s) = ln µθ (u | s) − ln|det ∇u g |.
Без доказательства.

(5)

■
Определение

Энтропией распределения π(a) называется

H(π(a)) := −Eπ(a) ln π(a).

Никита Юдин (МФТИ)

Лекция

(6)

16 апреля 2026

23 / 24

Пример
Например, для функции g (u) := tanh(u) якобиан ∇u g есть
диагональная матрица (поскольку преобразование поэлементное), и
его определитель равен покомпонентному произведению:
A

det ∇u g (u) = ∏ ∇ui tanh(ui ) =
i=1
A

= {производная гиперболического тангенса} = ∏(1 − tanh2 (ui ))
i=1

Заметим, что все компоненты положительные (tanh(ui ) ≤ 1), поэтому
модуль из формулы (5) брать не нужно. Подставляя в (5), получаем
окончательно:
A

ln πθ (a | s) = ln µθ (u | s) − ∑ ln(1 − tanh2 (ui )).
i=1

Никита Юдин (МФТИ)

Лекция

16 апреля 2026

24 / 24

